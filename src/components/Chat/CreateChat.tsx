"use client";

import Button from "../Shared/Buttons/Button";

export default function CreateChat() {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <Button variant="default"> Create chat</Button>
    </div>
  );
}
